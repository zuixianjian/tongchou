<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.exrick.xboot.your.mapper.RateConfigMapper">

    <select id="getDriverOrPassenger" resultType="cn.exrick.xboot.your.entity.RateConfig">
        SELECT * FROM b_rate_config WHERE del_flag=0
        AND b_rate_config.use_type=#{useType}
        AND b_rate_config.car_typea=#{carTypeA}
        AND b_rate_config.car_typeb=#{carTypeB}
        AND b_rate_config.fee_type=#{feeType}
        AND b_rate_config.region_group=#{regionGroup}
    </select>
    <select id="getCarloss" resultType="cn.exrick.xboot.your.entity.RateConfig">
        SELECT * FROM b_rate_config WHERE del_flag=0
        AND b_rate_config.use_type=#{useType}
        AND b_rate_config.car_typea=#{carTypeA}
        AND b_rate_config.car_typeb=#{carTypeB}
        AND b_rate_config.fee_type=#{feeType}
        AND b_rate_config.region_group=#{regionGroup}
    </select>
    <select id="getThirdDuty" resultType="cn.exrick.xboot.your.entity.RateConfig">
         SELECT * FROM b_rate_config WHERE del_flag=0
        AND b_rate_config.use_type=#{useType}
        AND b_rate_config.car_typea=#{carTypeA}
        AND b_rate_config.car_typeb=#{carTypeB}
        AND b_rate_config.fee_type=#{feeType}
        AND b_rate_config.insurance_money=#{insuranceMoney}
        AND b_rate_config.region_group=#{regionGroup}
    </select>
</mapper>